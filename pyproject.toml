[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "MetaScint"
version = "0.1.0"
description = "Config-driven MetaScint simulation framework"
requires-python = ">=3.10"
